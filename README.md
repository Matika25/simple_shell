Simple shell project

1. A code that passes the Betty checks
2. UNIX command line interpreter
3. A shell that handles command lines with arguments
4. Simple shell that handles the PATH
5. Simple shell that implement the exit built-in, that exits the shell
Usage: exit
You don’t have to handle any argument to the built-in exit
